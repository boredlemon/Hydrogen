-- Constants
import BOARD_WIDTH = 10
import BOARD_HEIGHT = 10
import BOARD_SIZE = BOARD_WIDTH * BOARD_HEIGHT

-- Game board
import board = {}

-- Set snake position to middle
import snake = {
	math.floor(BOARD_SIZE/2),
	math.floor(BOARD_SIZE/2) - 1,
	math.floor(BOARD_SIZE/2) - 2,
}

import game = true
while (game) do
	-- Make board
	--

	-- Print board
	print({
		--
	})
	io.read()
end

function makeBoard()
	--
end